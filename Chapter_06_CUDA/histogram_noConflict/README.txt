The CUDA source files contained here have timing statements inserted, in order to evaluate shich kernel runs faster. Otherwise, they are identical to the code in the book.

After compilation, run the program by passing as a command-line parameter the filename of a PGM image. Example:

	./histogram mountains.pgm

